<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hamidur.mynews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewsActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name" />
        <activity
            android:name=".SourceActivity"
            android:label="@string/all_source_activity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_activity" />
        <activity
            android:name=".OrderByActivity"
            android:label="@string/order_activity" />
        <activity android:name=".WeatherActivity"
            android:screenOrientation="portrait"
            android:label="@string/my_weather"></activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/about_activity"/>

    </application>

</manifest>